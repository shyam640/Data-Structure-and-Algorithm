#include<bits/stdc++.h>
using namespace std;
int main(){
   /********* Taking Input in graphs ********/
   // int n,m;
   // cin>>n>>m;
   // int u,v;
   // for(int i=0;i<n;i++){
   //    cin>>u>>v;
   // }

   /************* Undirected Graph Storing **********/
   /******* Adjacency Matrix *******/
   // int n,m;
   // cin>>n>>m;
   // int u,v;
   // int adj[n+1][n+1];
   // for(int i=0;i<n;i++){
   //    cin>>u>>v;
   //    adj[u][v] = 1;
   //    adj[v][u] = 1;
   // }

   /************ Directed Graph Storing **********/
   /******** Adjacency Matrix ***********/
   // int n,m;
   // cin>>n>>m;
   // int u,v;
   // int adj[n+1][n+1];
   // for(int i=0;i<n;i++){
   //    cin>>u>>v;
   //    adj[u][v] = 1;
   // }

   /************* Undirected Graph Storing **********/
   /******* Adjacency List *******/
   // int n,m;
   // cin>>n>>m;
   // int u,v;
   // vector<int> adj[n];
   // for(int i=0;i<n;i++){
   //    cin>>u>>v;
   //    adj[u].push_back(v);
   //    adj[v].push_back(u);
   // }

   /************ Directed Graph Storing **********/
   /******** Adjacency Matrix ***********/
   // int n,m;
   // cin>>n>>m;
   // int u,v;
   // vector<int> adj[n];
   // for(int i=0;i<n;i++){
   //    cin>>u>>v;
   //    adj[u].push_back(v);
   // } 


   /************* Undirected Graph With Given Weights **********/
   // int n,m;
   // cin>>n>>m;
   // int u,v,w;
   // vector<pair<int,int>> adj[n];
   // for(int i=0;i<n;i++){
   //    cin>>u>>v>>w;
   //    adj[u].push_back({v,w});
   //    adj[v].push_back({u,w});
   // }
}